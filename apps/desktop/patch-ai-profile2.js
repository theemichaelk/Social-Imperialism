const fs = require('fs');
let file = "index.js";
let content = fs.readFileSync(file, "utf8");

let additionalCode = `\nipcMain.handle('get-ai-intelligence-profile', async (event, accountId) => {\n  const activeCampaignId = store.getItem('activeCampaignId') || 'default';\n  const data = store.getItem('linkedAccounts_' + activeCampaignId);\n  let accounts = [];\n  if (data) {\n    try { accounts = JSON.parse(data); } catch(e) {}\n  }\n  \n  const account = accounts.find(a => a.id === accountId);\n  if (!account) return { error: 'Account not found' };\n\n  try {\n    const prompt = 'You are an expert Social Media Intelligence AI.\nGenerate a realistic AI Intelligence Profile for the ' + account.platform + ' account "' + account.handle + '".\nReturn EXACTLY a JSON object with this structure (no markdown tags outside):\n{\n  "followers": "Integer number of followers e.g., 14500",\n  "likes": "Integer number of total likes e.g., 89000",\n  "bestTime": "String like 'Tuesday 2:00 PM EST'",\n  "topTrendingNiche": "String like '#TechStartups'",\n  "growthVelocity": "String like '+4.2%'",\n  "suggestedGroups": ["Group 1", "Community 2"]\n}';\n    \n    const aiResp = await generateAI(prompt);\n    let cleanJson = aiResp.trim();\n    if(cleanJson.startsWith('```json')) cleanJson = cleanJson.substring(7);\n    if(cleanJson.endsWith('```')) cleanJson = cleanJson.substring(0, cleanJson.length - 3);\n    \n    const profile = JSON.parse(cleanJson.trim());\n    \n    // Update account with new profile\n    account.profile = profile;\n    store.setItem('linkedAccounts_' + activeCampaignId, JSON.stringify(accounts));\n    \n    return profile;\n  } catch (err) {\n    console.error('AI Intelligence Profile Error:', err.message);\n    return account.profile || { error: 'Failed to generate profile' };\n  }\n});`;

content += additionalCode;

fs.writeFileSync(file, content);
console.log('Success');